#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define sz(x) (int)(s.size())

void kdimo() {
  
}

int main() {
  ios_base::sync_with_stdio(false);  cin.tie(NULL);  cout.tie(NULL);

  int t = 0; 
  cin >> t;
  while (t--) {
    kdimo();
  }

  return 0;
}
